# 🔍 Система семантического поиска по корпоративной базе знаний

## 📝 Описание проекта
Данный веб-сервис реализует технологию семантического поиска в базе данных при помощи NLP (Natural Language Processing) и векторного представления данных.

## 🚧 Проект находится в стадии разработки

## ⚙️ Стек технологий
* **Язык:** Python 3.10+
* **ML / NLP:** `sentence-transformers` (модель: `google/embeddinggemma-300m`)
* **Бэкенд:** Uvicorn + FastAPI
* **База данных:** SQLite
* **Фронтенд:** Jinja2, HTML+CSS+JavaScript

## 🚀 Установка и запуск

#### ❓ Для использования модели `google/embeddinggemma-300m` (выбрана по-умолчанию) может потребоваться авторизация в HuggingFace Hub. Следуйте инструкциям при запуске. Если такой возможности нет, можно выбрать другую модель в `load_model` (`main.py:43`).

#### ❗ Если вы поменяли модель, то необходимо удалить базу данных (`/app/data.db`). Это обусловлено несовместимостью эмбеддингов разных моделей.

### [Вариант 1] Менеджер окружений (рекомендуемый)
1. Установка `uv`: [Инструкция](https://docs.astral.sh/uv/getting-started/installation/)
2. Подключение виртуального окружения и скачивание зависимостей:
```sh
uv sync
```
3. Запуск:
```sh
cd app
# С доступом только на локальном хосте:
uv run uvicorn main:app --port 80
# Или запуск на открытом хосте (с доступом из сети):
uv run uvicorn main:app --port 80 --host 0.0.0.0
```

### [Вариант 2] Legacy (не рекомендуется)
1. Создание виртуального окружения:
```bash
python -m venv .venv
```
2. Активация окружения:
* Windows: `.venv\Scripts\activate`
* macOS/Linux: `source .venv/bin/activate`
3. Установка зависимостей:
```bash
pip install -r requirements.txt
```
4. Запуск:
```sh
cd app
# С доступом только на локальном хосте:
uvicorn main:app --port 80
# Или запуск на открытом хосте (с доступом из сети):
uvicorn main:app --port 80 --host 0.0.0.0
```

### Запуск в Docker
0. (опционально) Предварительно загрузить Docker образ из DockerHub: \
`docker pull jwth32/semantic-ss`
1. Запустить контейнер с нужными параметрами: \
`docker run -e HF_TOKEN=<ВАШ_ТОКЕН_HUGGINGFACE> -d -p 80:8000 jwth32/semantic-ss [OPTIONAL_ARGS]` \
_При запуске через Nginx лучше не давать контейнеру порт 80 напрямую_ \
Список поддерживаемых аргументов с их значениями по-умолчанию: \
`--host 0.0.0.0` - Хост (0.0.0.0 для доступа из сети) \
`--port 8000` - Порт в докере \
`--database sqlite+aiosqlite:///data.db` - URL базы данных (можно использовать какую-то внешнюю) \
`--model google/embeddinggemma-300m` - Модель SentenceTransformers с HuggingFace \
Для модели от Google нужен токен, привязанный к учётной записи HuggingFace.

### Тестирование и использование

Подключение:
1. Веб-интерфейс: перейти по адресу `127.0.0.1` (Перед этим нужно импортировать базу) \
    1. По адресу `127.0.0.1` находится страница поиска
    2. По адресу `127.0.0.1/dashboard` находится панель управления, где можно инициализировать базу значениями по-умолчанию
2. Взаимодействие с API в браузере: `127.0.0.1/docs` (Swagger)

## 📁 Структура проекта
```text
Semantic_Search_System/
│
├── data/                   # Синтетические данные (документы для загрузки)
├── app/
│   ├── ml/
|   |   └── ml_engine.py    # Логика векторизации и косинусного сходства
|   |
|   ├── database/
|   |   └── database.py     # Сохранение и чтение документов
|   |
|   ├── frontend/
|   |   ├── static/         # Статические файлы: скрипты, стили, медиа
|   |   ├── templates/      # HTML шаблоны
|   |   └── frontend.py     # Эндпоинты для браузера
|   |
|   └── main.py             # Основной файл FastAPI (эндпоинты)
|
├── docs
│   ├── plan.md             # План разработки и взаимодействия
│   └── responsibilities.md # Распределение обязанностей
├── requirements.txt        # Список зависимостей Python
├── pyproject.toml          # Конфигурация окружения для менеджеров (uv)
└── README.md               # Документация проекта
```