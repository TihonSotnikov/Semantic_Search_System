# Отчёт о проделанной работе #5: 12.05 – 18.05

## Команда:
- Червов Андрей
- Сотников Тихон

## Проделанная работа:
1. __Т. Сотников__: Выбраны подходящие метрики:
    - Recall@k - Какую долю из существующих релевантных ответов выбирает в top-k
    - NDCG - Метрика, учитывающая релевантность и позицию в топе
2. __А. Червов__: [Pull Request](https://github.com/TihonSotnikov/Semantic_Search_System/pull/8)
- Переработана точка входа приложения.
- Теперь можно запускать его как через `uvicorn main:app [args]`, так и через `python main.py [args]`.
- Добавлена конфигурация через аргументы при помощи `argparse`. Аргументы `--port` и `--host` передаются в `uvicorn.run`.
- Создан рабочий Dockerfile для сборки образа приложения: `/Dockerfile`.
    - Для запуска сервиса со стандартными настройками нужно при создании контейнера передать токен HuggingFace через переменную окружения: \
    `docker run -d [...] -p 80:8000 -e "HF_TOKEN=<token>" jwth32/semantic-ss`

Далее в планах: 
- __Сотников Тихон__: Добавить логгирование и генерацию документации.
- __Червов Андрей__: Протестировать и загрузить образ на DockerHub. Написать тесты. Додокументировать свой код.
- __Совместно__: Подготовка к защите.
